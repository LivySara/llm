# LLM

## 弊端

- 只说不做
- 无记忆
- 不会用工具
- 不会规划

# Agent

## 解决LLM弊端

- 【只说不做】- 引入工具调用
- 【无记忆】- 配备记忆系统
- 【不会用工具】- 业界推出了 MCP 等标准化协议来统一工具接入方式
- 【不会规划】- 具备了任务拆解和规划能力

## 核心组成

- LLM(大脑)：负责理解意图、推理判断、决定下一步行动
- 规划模块：负责把复杂任务拆解成可执行的步骤
- 记忆模块：负责存储和检索信息，让 Agent 能在长时间任务中保持连贯
- 工具模块：是 Agent 的「手和脚」，让它能跟外部世界互动

## 工作模式

- ReAct(边想边做)
- Plan-and-Execute(先想好再做)
- Reflection(做完再检查)
- Multi-Agent(团队协作)

# Workflow

## 与agent区别

- 核心区别：谁在控制流程
前者按照规定好的顺序执行（无自由）
后者按照自己做出的决策执行（自由）

# Function Call

## 工作原理

- 定义函数：LLM有那些工具用
- 模型判断：LLM分析用户意图，决定调用那些工具
- 执行函数：【注意：LLM并不执行函数】开发的代码执行
- 生成回答：将执行结果发给LLM

## 与agent关系

- 是agengt能力的最底层技术基础
- Function Call 是一次性的「单步调用」；Agent 是「循环调用」，Agent 在一个循环中反复使用 Function Call，每次调用后观察结果，再决定下一步要不要继续调用其他函数

# MCP

## 背景

- 每个 LLM 厂商的 Function Call 格式还不完全一样，参数结构也有差异

## 解决问题

- 以前每个 AI 应用要跟每个服务单独对接，现在每个 AI 应用只要支持 MCP 协议（实现一次 Client），每个服务只要提供一个 MCP Server（实现一次 Server），双方就能自动对接 🤔？
- MCP 让 Agent 用统一标准连接工具

# Skills

## 定义
- 一种自然语言指令文件，通常是 Markdown 格式，用来教 Agent"在什么场景下、按照什么方法、遵循什么规范来完成特定任务"
- 本质是"知识 + 行为指令"，它通过加载到上下文中来改变 Agent 的思考和行为方式，同时可以驱动 Agent 去调用 Function Call、MCP 工具、执行代码等外部操作

# A2A协议

## 背景

- 多个agent之间如何通信

## 技术

- 基于现有Web标准，HTTP用于传输、JSON用于消息格式、SSE用于实时流式通信
- 支持异步和长时间运行的任务
- 与模态无关